import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  message: {
    flexDirection: 'row',
    maxWidth: 300,
    backgroundColor: 'red',
    borderRadius: 5,
    paddingHorizontal: 5,
    paddingVertical: 10,
  },
});
